"# BoliRealEstateApp" 

ionic capacitor add android
ionic capacitor run android
npx cap --version
ionic capacitor --version
npm list @capacitor/core
npx cap --version
npx capacitor add ios 
npx cap open ios

generated apk file also uploading it in the code section
